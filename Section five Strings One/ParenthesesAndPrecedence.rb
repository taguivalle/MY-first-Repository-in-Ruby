def authenticate_agent(rank, name, credentials)
    if (rank == "007" && name == "James Bond") || credentials == "Secret Agent"
        puts "Acces granted, please proceed to Intelligent department!"
    else
        puts "Acces denied, #{name}"
    end
end
authenticate_agent("007", "James Bond", "Spy") # Note that there is another credential here
puts
def authenticate_agent(rank, name, credentials)
    if (rank == "007" && name == "James Bond") || credentials == "Secret Agent"
        puts "Acces granted, please proceed to Intelligent department!"
    else
        puts "Acces denied, #{name}"
    end
end
authenticate_agent("007", "Jim Bond", "Spy") # Note that there is another credential here
puts
def authenticate_agent(rank, name, credentials)
    if (rank == "007" && name == "James Bond") || credentials == "Secret Agent"
        puts "Acces granted, please proceed to Intelligent department!"
    else
        puts "Acces denied, #{name}"
    end
end
authenticate_agent("007", "Jim Bond", "Secret Agen") # Note that there is another credential here
puts
def authenticate_agent(rank, name, credentials)
    if (rank == "007" && name == "James Bond") || credentials == "Secret Agent"
        puts "Acces granted, please proceed to Intelligent department!"
    else
        puts "Acces denied, #{name}"
    end
end
authenticate_agent("006", "Alek Trevelyan", "Secret Agent") # note that here there is another name and another rank
puts
